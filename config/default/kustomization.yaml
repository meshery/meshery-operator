# Adds namespace to all resources.
namespace: meshery

# Value of this field is prepended to the
# names of all resources, e.g. a deployment named
# "wordpress" becomes "alices-wordpress".
# Note that it should also match with the prefix (text before '-') of the namespace
# field above.
#namePrefix:

# Labels to add to all resources and selectors.
#commonLabels:
#  someName: someValue

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../crd
- ../rbac
- ../manager
# [WEBHOOK] Conversion webhook Service (v1alpha1 <-> v1alpha2 hub).
- ../webhook
# [CERTMANAGER] Self-signed Issuer + serving Certificate for the webhook. Requires
# cert-manager to be installed in the cluster.
- ../certmanager
# [PROMETHEUS] To enable prometheus monitor, uncomment all sections with 'PROMETHEUS'.
#- ../prometheus

# Metrics are served by the manager itself on :8080 (see config/manager).
# The legacy kube-rbac-proxy sidecar was removed (its image was retired and the
# sidecar pattern is deprecated); WS-5 reintroduces metrics protection via the
# controller-runtime authn/authz filter and cert-manager-issued TLS.

# [WEBHOOK] Mount the cert-manager-issued serving cert into the manager and expose
# the webhook port. (No admission webhooks exist, so webhookcainjection_patch.yaml
# is not used; the CRD conversion CA is injected via the cainjection CRD patches.)
patches:
- path: manager_webhook_patch.yaml
