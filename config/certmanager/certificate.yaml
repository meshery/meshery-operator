# A self-signed Issuer and the serving Certificate for the operator's webhook.
# Targets the cert-manager.io/v1 API (cert-manager >= 1.0); docs at
# https://cert-manager.io/docs/
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: selfsigned-issuer
  namespace: meshery
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: serving-cert  # this name should match the one appeared in kustomizeconfig.yaml
  namespace: meshery
spec:
  # DNS names of the webhook Service (meshery-webhook-service in namespace
  # meshery). The Certificate itself is namePrefixed to meshery-serving-cert.
  dnsNames:
  - meshery-webhook-service.meshery.svc
  - meshery-webhook-service.meshery.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: selfsigned-issuer
  secretName: webhook-server-cert # this secret will not be prefixed, since it's not managed by kustomize
